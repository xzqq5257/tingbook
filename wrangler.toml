name = "tingbook"
pages_build_output_dir = "."
compatibility_date = "2024-11-01"

# KV 绑定（TINGBOOK_KV）：Cloudflare Pages 该项目由 wrangler.toml 管理绑定，
# 故这里写真实 namespace ID（非占位），推上去后 Cloudflare 据此注入运行时。
[[kv_namespaces]]
binding = "TINGBOOK_KV"
id = "b67754879ac74bd79ec31e7f57bfc57d"
